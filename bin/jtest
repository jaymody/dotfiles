#!/bin/bash

# check if play dir exists
PLAY_DIR="${HOME}/code/play"
if [ -d "$PLAY_DIR" ]; then
	cd ${PLAY_DIR}
else
	echo 'error, could not find ${PLAY_DIR}'
	exit 1
fi

# start session
mkdir $1
TERM=xterm-256color
jupyter lab
