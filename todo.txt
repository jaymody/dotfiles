- redesign the way setup/install works (add options, modularity, seperation of concerns)
- linux
- add install option on linux when sudo priveleges are absent
- add install option on linux when gui is not avaliable (server environment)

- macos defaults/settings (https://github.com/mathiasbynens/dotfiles/blob/master/.macos)
- figure out a way to automatically save and load iterm2 settings
- figure out a way to automatically save and load btt settings
- automate the calling of "./update" every couple days
- vscode autosetup
- vscode update only works on mac
- centralize tree command ignores (.git, .ipynb_checkpoints, etc ...)

non-portable commands:
- open vs xdg-open
- pbcopy vs xclip
- gls --group-directories-first vs ls --group-directories-first
